<div class="filter-item">
  <div class="filter-title">{{title}}</div>
  <div class="dropdown-list-sm">
    <button id="dLabel" type="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false" ng-click="changeFilterDisplayStatus({ev: $event})">{{(value[mapto]) || "Keyword"}} <span class="icon down-arrow"></span> </button>
    <ul class="dropdown-menu" aria-labelledby="dLabel">
      <li ng-repeat="(key,value) in listobj" ng-click="setFilter({ ev: $event, select: '{{value[mapto]}}', type: '{{mapto}}' })" ><a href="javascript:void(0);">{{value[mapto]}}</a></li>
    </ul>
  </div>
</div>
